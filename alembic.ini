[alembic]
script_location = %(here)s/alembic
prepend_sys_path = %(here)s
path_separator = os
output_encoding = utf-8

# Database URL comes from ProjectSettings; no second production default here.
# Logging remains owned by the application; env.py does not call fileConfig.
