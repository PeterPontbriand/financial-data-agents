# ESC-A — Coverage and acceptance matrix

**Status:** Populated investigation inventory, not final acceptance. Revision `8d7fba0`; fresh full gate passed 2,004 tests / 89% coverage. [Evidence and contracts](ESC_A_EVIDENCE_AND_REPAIR_CONTRACT.md), [ledger](ESC_A_DEFECT_LEDGER.md), [authority](EXISTING_STRATEGY_CORRECTNESS_PLAN.md).

“Existing” below means relevant tests/source were identified and the complete suite passed; it does not claim those tests exhaust the listed dimension. “Required” identifies explicit ESC-B/C proof still owed. All applicable cells must be reconciled before ESC-C. A missing CLI capability is marked non-applicable only at the CLI boundary, not silently excluded from service/tool tests.

## Per-analysis coverage

| Dimension | Graham Number | Graham Growth | Momentum | FCF/Earnings Growth |
| :--- | :--- | :--- | :--- | :--- |
| Modes and status | Existing Graham presenter/CLI suites; live details+JSON succeed. Required C1–C3/C6 all four modes, optional quote failure versus calculation failure. | Existing Growth/nonpositive presentation suites; live details+JSON succeed. Required override reasons and comparison/failure parity. | Existing presenter/CLI suites; live details+JSON fail on invalid provider data. Required ESC-06/07/11/12 plus successful valid live history. | Existing CLI/classification suites; live details+JSON succeed. Required C3/C5/C6 basis-aware statements and optional-status details. |
| Cold/hit/bypass/refresh | Existing financial-cache and comparison composition tests; isolated live first call cold, later hit. Required finite quote policy, refresh once, refresh failure and valid annual-cache reuse. | Same shared cache; required price/yield/EPS override isolation and quote policy through Growth service/tool. | Existing exact-request cached-client and CLI-cache tests; new reproduction proves source/trace loss. No CLI bypass flag; injected client is the applicable bypass boundary. Required original timestamp retention on hit/expiry. | Existing annual-series cache/repository tests; live second call hit. Required notes/lineage/cache-origin parity, field refresh and provider failure. No quote policy applies. |
| Missing/legacy/corrupt/future cache | Existing repository payload validation. Required quote unknown/future retrieval, legacy synthetic observation time, corrupted payload, expiry reasons without fabricated cause. | Same; prove failure does not substitute stale price or discard independently valid valuation. | Existing repository frame/metadata checks. Required legacy missing fetch time, future cached time disposition, invalid frame rejection and typed failure output. | Existing series cache schema/temporal tests. Required corrupt/missing series and partial field evidence propagate accurately in all modes. |
| Time/historical | Existing availability/restatement/unit tests. Required cutoff equality, timezone offsets, current quote excluded from historical requests, historical override labels. | Same plus selected EPS basis and user forecast/yield labels. | Existing tool `as_of` filtering; CLI `--as-of` not supported. Required same-day labels, future rows, invalid timestamps, adjusted-price vintage limits; no invented session-close knowledge. | Existing SEC annual alignment, foreign-form and snapshot tests. Required cutoff/fiscal/restatement composition and effective-as-of consistency across CLI/direct/tool. |
| Invalid and missing inputs | Existing positive EPS/BVPS and security-unit checks. Required zero/negative/nonfinite overrides, currency/unit mismatch, partial profile, unsupported class/ETF, lineage rejection. | Existing nonpositive-growth tests. Required growth=0, negative growth/value, AAA yield≤0, percentage-point units, no percentage comparison for nonpositive reference. | Existing hardening tests. Required supplied/fetched frame parity, NaN/Inf anywhere, ordering/duplicates, zero/no-event distinctions, unequal/equal SMA transitions, insufficient history. | Existing calculator/resolver/model/classification tests. Required OCF/CapEx sign, missing versus zero shares/FCF/EPS, nonpositive bases, selected basis and forward-policy combinations. |
| Independent arithmetic | Captured unrounded EPS/BVPS/Graham result and quote comparison independently agree. Required varied fixtures and rounding boundaries, dimensioned filings. | Captured formula agrees using explicit 5 and 4.4 assumptions. Required defaults/alternative basis/negative boundary oracles. | Three-point SMA/RSI arithmetic agrees but crossover claim is wrong. Required independent SMA, existing simple RSI, spread and crossover oracles across boundaries. | Six captured FCF and per-share derivations plus three CAGRs agree. Required opposite total/per-share directions, horizons 3/4/5, nonmeaningful spans and classification truth table. |
| Source/composition | Retained real KO filing repair. Required mixed aggregate/dimensional disclosures, treasury/issued and inferred-zero lineage, no source accession/value substitution. | Same source-unit contract, plus provider/basis routing and explicit assumptions. | Offline CLI/cache reproduction with one fetch; live invalid last row reproduced. Required provider-shaped missing OHLC, normal history, transport failure and cache provenance. | Existing SEC parser/integration fixtures cover OCF/CapEx/EPS/shares. Required all four modes using real parser/facade/resolver/classifier with reduced transport fixtures, not fabricated result objects alone. |
| JSON/direct/tool compatibility | Current presentation v4 → proposed v5. Required defaults on additive fields, service `PriceComparison` evidence and legacy interface tests. | Same v4 → v5; preserve aliases and tool argument constraints. | Current v3 → v4. Required transient resolution serialization, crossover compatibility projection, retained tool trace and failure JSON. | Presentation v4 → v5; canonical result v3/method v2 retained. Required typed numeric nulls, optional metric reasons, classification and tool serialization. |

## Existing regression anchors

Paths are repository-relative. These are concrete starting points for extension; final acceptance must name the actual added/changed tests and their results.

| Area | Existing files/directories |
| :--- | :--- |
| Graham math/resolution | `tests/analysis/graham_value/test_calculators.py`, `test_resolver.py`, `test_bvps_basis_semantics.py`, `test_sec_bvps_hardening.py`, `test_security_unit_compatibility.py` |
| Graham real composition/CLI | `tests/test_graham_comparison_composition.py`, `tests/test_cli_graham_commands.py`, `tests/test_cli_graham_nonpositive_growth.py`, `tests/test_cli_graham_slice_f_routing.py`, `tests/test_cli_financial_cache.py` |
| Security source/profile | `tests/data/test_sec_security_unit.py`, `test_security_unit.py`, `test_instrument_profile.py`, `test_provider_security_identity.py`, `test_yfinance_financial_facts.py` |
| Momentum | `tests/analysis/momentum/test_momentum_analyzer.py`, `test_momentum_hardening.py`; `tests/reporting/test_momentum_presenter.py`; `tests/test_cli_historical_cache.py`; `tests/data/test_cached_client.py`, `test_yfinance_client.py`, `test_quality.py` |
| FCF | `tests/analysis/fcf_earnings_growth/` calculator, classification, resolver, analyzer, SEC integration, foreign annual forms, IFRS duration facts, diluted shares, identity/unit and production-composition suites; `tests/test_cli_fcf_earnings_growth.py` |
| Persistence/consumers | `tests/data/repositories/test_resolved_input_cache.py`, `test_series_cache.py`, `test_market_data.py`; `tests/orchestrator/test_analysis_tools.py`; `tests/evaluation/cases/` and `tests/evaluation/fixtures/` |

## Absence/zero inventory and required assertions

| Surface | Values requiring a disposition | Verification rule |
| :--- | :--- | :--- |
| Shared identity/profile | Name, listing venue, kind, provider, classification time | Selected source may omit fields; distinguish current metadata from filing evidence. No fabricated enrichment. |
| Graham financial details | Basis, provider field, fiscal start/end, point observation, filing availability, component notes, preferred zero | C1/C3 terminology by fact type. Inference and component lineage remain visible on cache hits. All monetary values retain currency. |
| Graham comparison | Missing quote, incompatible/unsupported units, missing lineage, currency, nonpositive reference | Stable reason and null percentage. Explain refresh only when it can help; an unsupported class must not receive a misleading refresh remedy. |
| Momentum | Missing SMA/RSI/crossover, null spread, unknown trend, observation count/date/interval, currency/adjustment | Independent window counts. Crossover zero requires two valid states. A missing source field is not a computed zero. Historical date is not a current quote timestamp. |
| FCF | Period/horizon absent, zero observation count, unavailable/nonmeaningful CAGR, missing diluted shares, consensus/yield nulls | Retain exact reason and selected basis. Counts can legitimately be zero on input failure. No unapproved forward or market-cap substitution. |
| JSON | Null metadata/metrics, zero numbers, empty diagnostics, schema versions | Nulls remain numeric absence; no NaN/Inf. Available evidence must not be lost during composition. Error output is a parseable versioned document under C6. |

## Outstanding acceptance work and closure rules

1. Add permanent realistic regression fixtures for all twelve ledger entries, including the reduced KO last-row data failure; execute the repair contracts only after approval.
2. Complete each applicable matrix cell with a concrete test, source proof or dated live observation. Investigate current open audit questions about future cache times, historical daily boundary semantics, FCF effective-as-of propagation and corrupt-entry presentation. A discovered additional defect enters the ledger before closure; this list is not an approved waiver.
3. Obtain successful representative live output for all four analyses, plus documented unavailable/error cases. Current Momentum live failure is explained but does not satisfy its success cell. Live provider recovery is not assumed.
4. Reconcile supported provider/basis combinations and public interfaces against the implemented behavior; update dated user examples only after their outputs are verified.
5. Run the full managed gate and record current revision, exact totals/coverage and unique artifact directory. Require ≥85% overall coverage and meaningful branch evidence, not only a test-count increase.
6. Present one ESC-C review with no unresolved known defects or unexplained required cells. Preserve the ESC-D revalidation requirement on the actual Step 3.5 starting revision after readiness/workspace/profile work.
