# ESC-A — Defect ledger

**Source revision for every entry:** `8d7fba0`, including Graham implementation `e8f4a95`. **Disposition:** all entries remain open for repair/verification; no ESC-C acceptance is claimed. **Repair commit:** none in this stage. The earlier Graham parser repair is retained under ESC-05, not relabelled as newly implemented work.

Severity means impact within this application: **high** for misleading financial/data claims or inconsistent validation, **medium** for missing/misleading explanations or failure contracts. These are not security severity ratings. Contract references and exact file groups are in the [repair contract](ESC_A_EVIDENCE_AND_REPAIR_CONTRACT.md). Every closure must add repair revision, permanent regression names, relevant live evidence and acceptance disposition here.

## Confirmed findings

| ID | Scope/severity/impact | Evidence, actual behavior and root cause | Expected contract and closure evidence |
| :--- | :--- | :--- | :--- |
| ESC-01 | Both Graham methods; all modes and consumers; **high** | Original pasted run reused a quote 13h51 old. Offline `financial_cache_eligible` reproduction accepts a 14-hour-old quote with `ttl=None`. `ProjectSettings.financial_cache_ttl_seconds` defaults to unlimited. Yahoo copies retrieval into observation/availability, although exchange time is not retained. | C1. Independent quote reuse age, preserved retrieval, honest unknown market time, refresh failure without stale fallback, override/historical cases. Test 299/300/301 seconds, zero TTL, missing/future times, legacy cached metadata, stricter general TTL and all consumers. |
| ESC-02 | Graham filing metadata, shared identity wording; **medium** | Live KO details report venue unavailable. SEC's share-unit parser verifies symbol/exchange but `ParsedUnitDocument` and `SecurityUnitDocument` discard exchange text. Selected SEC current identity lacks venue. | C2. Preserve verified filing venue with its own source/time; no unsupported current-identity merge. Tests for partial identity, document disagreement, unrelated exchange dimensions, unsupported mapping and multiple documents. Live KO must show filing venue separately. |
| ESC-03 | All four analyses; details/concise/JSON absence semantics; **medium** | Live Number says quote basis unspecified and fiscal periods unavailable; derived EPS/BVPS provider field is n/a; Growth user AAA assumption has “freshness unavailable.” Generic helpers conflate point data, derivation, assumptions and missing metadata. | C3 plus C1. Field-by-field missing/inapplicable/user-supplied reasons, unchanged numeric nulls. Matrix covers identity, currency, dates, source fields and optional metrics; verify JSON/text agreement rather than deleting every “unavailable.” |
| ESC-04 | Number BVPS lineage; details/cache presentation; **high** | Live preferred-share component is 0 with `provider_field=inferred:sec-company-facts:no-issued-preferred-equity` but source says derived. `_source_label` recognizes inferred prefixes only for PROVIDER while new typed lineage uses DERIVED. Child notes and issued-minus-treasury grandchildren are omitted from details. | C3. Recursive lineage with inference label/guard notes and component units. Cache/fresh parity; explicit-zero versus guarded-inference versus missing/contradictory preferred evidence. Financial assumptions and zero guard stay unchanged. |
| ESC-05 | Graham parser and cross-analysis composition evidence; **high** | Earlier KO repair missed routine dimensioned equity disclosures. `e8f4a95` already excludes nonaggregate candidates without accepting unsupported share classes. Fresh Number/Growth now succeed, but this alone does not establish four-analysis acceptance. | Preserve parser negative/positive tests and extend C1–C6 realistic composition coverage. Each analysis needs source-shaped fixtures, independent arithmetic, failure cases and dated live checks. Close only after matrix reconciliation. |
| ESC-06 | Momentum calculation/direct API/all modes; **high** | Close `[1,2,3]`, short=2, long=3 produces SMAs 2.5/2.0 and crossover +1. Previous long SMA is missing. `np.where(short > long, 1, 0)` treats undefined prior comparison as zero before differencing. Offline reproduction confirms. | C4. Trend may be bullish, but crossover must be unavailable until two valid SMA pairs exist. Test exactly long-window bars, one additional bar, true bullish/bearish transitions, equality and no-event zero. Preserve SMA/RSI formulas. |
| ESC-07 | Momentum CLI diagnostics and JSON; **medium** | Offline real CLI/resolver/cache composition yields four retained run trace events but JSON `diagnostics=[]` with an empty profile. CLI constructs `MomentumPresentation` without passing `run.resolution_trace`. | C4. Retain actual resolver/cache/derivation events in CLI modes and direct/tool consumers. Test provenance events, empty/partial profile diagnostics and no duplicated trace. |
| ESC-08 | Momentum cached input provenance; **high** | Three offline runs use one provider fetch. A cached run nevertheless labels every retained price input PROVIDER, with retrieval time taken from the later resolver clock. Cache client returns only `entry.data`, discarding stored retrieval/cache timing. | C4. Transient typed resolution metadata from existing entry timestamps, correct cache origin and original retrieval. Test separate clocks, legacy missing retrieval, cold/hit/expiry/bypass and persistence round trip without schema migration. |
| ESC-09 | FCF per-share classification explanation; **high** | Six annual fixture observations: FCF 80→130, shares 100→600, EPS 2→4.5. Per-share mode correctly computes total FCF CAGR +10.20%, per-share −22.99%, EPS +17.61%, and FAIL, but says “Diluted EPS increased, but free cash flow did not.” `_TREND_LABELS` and the generic failure reason omit selected basis. | C5. Say FCF per diluted share did not grow. Test both bases with opposing total/per-share growth and every classification/forward-policy state. No reclassification or formula change. |
| ESC-10 | FCF detail evidence; **medium** | Live OCF/CapEx/EPS lines show `currency` or `currency_per_share` without their currency identifier. `_input_details` omits source kind/cache origin, notes and nested lineage. JSON has the omitted evidence. Derived totals elsewhere show USD, but that is not complete component provenance. | C3. Render retained currency, readable units, cache origin and notes consistently. Test non-USD synthetic facts, mixed/incompatible inputs rejected by resolver, zero values and derived CapEx. No currency conversion. |
| ESC-11 | Execution failures, directly reproduced in Momentum; **medium** | KO's 2026-09-10 historical OHLC is missing. Both details and JSON exit 1; JSON stdout is empty. Generic `execution_errors` loses the actionable quality detail; only a generic quality log and no-usable-history message remain. | C6. Typed sanitized failure JSON and text diagnostics with field/date, preserving nonzero status. Test provider failure, invalid data, unexpected exceptions and cache errors on all four commands, parser exit 2, no secret/raw payload leakage. Rejecting NaN itself remains correct. |
| ESC-12 | Momentum public preloaded-frame API; **high** | Close `[NaN,2,3,4]`, dated four consecutive days, short=2/long=3/RSI=2 returns BULLISH and SMAs 3.5/3.0. `run_analysis` validates only when `df is None`; rolling windows can hide invalid earlier observations. Normal provider route rejects the same frame. | C4. Common input validation before calculation for both routes. Test NaN/Inf inside/outside trailing windows, empty/missing Close, duplicate/out-of-order dates and valid supplied frames. Do not drop rows to make a result available. |

## Durable reproduction recipes

These recipes identify the public boundaries and complete distinguishing inputs. Use the already synchronized environment and isolated temporary directories; transport must be blocked except for explicitly dated live checks.

1. **ESC-01:** At `2026-09-11T00:00:00Z`, create a provider `ResolvedInput` for `current_price=87.55`, units `currency_per_share`, currency USD, provider `yfinance`, with resolved/retrieved/observed/available times fourteen hours earlier. Call `financial_cache_eligible(value, input_id="quote", now=boundary, as_of=None, cached_at=old_time, ttl=None)`. Actual: `True`.
2. **ESC-06/12:** Call `MomentumAnalyzer().run_analysis(MomentumConfig(short_window=2, long_window=3, rsi_period=2), ticker="ACME", df=frame)` with the Close values and consecutive January 2026 dates above. No fetch is needed. Actual crossover +1 for the three-point fixture; NaN-containing four-point fixture accepted.
3. **ESC-09:** Start with `annual_series(range(2020, 2026))` from `src/evaluation/fixtures/fcf_earnings_growth.py`; replace each fact's provider with `SEC_PROVIDER_ID`, and each weighted-average-diluted-shares value with `100 * (fiscal_year - 2019)`. Feed `FixtureAnnualFinancialFactsProvider` into the real `ProductionAnnualGrowthSeriesResolver`, clock `2026-09-11T00:00:00Z`, then `FCFEarningsGrowthAnalyzer`, USD, no as-of, no cache, per-share policy. Render concise using the real presenter. The source-shaped fact fixture passes actual selection/derivation/classification; no result was mocked. It is not a transport-level SEC parser fixture, which remains required at ESC-B.
4. **ESC-07/08:** Follow the real isolated-SQLite pattern in `tests/test_cli_historical_cache.py`, using `momentum_success_frame()`, provider `yfinance`, interval `1d`, adjustment `adjusted`, USD and consistent row count/date. Patch only historical retrieval and optional profile acquisition; retain real cache, resolver, analyzer, presenter and CLI. Invoke twice with short=2/long=3/RSI=3 and JSON, then call `run_with_context` against the cached client. One fetch serves all three runs, four run trace events exist, CLI diagnostics are empty, retained inputs say provider. This exposes missing evidence, not a database-readiness failure.
5. **ESC-11:** The live source probe used `YFinanceClient().fetch_data_with_context("KO", "2021-01-01")`, then `evaluate_historical_quality`. All rows were retained. Numeric quality failed; index/context/currency/adjustment passed; market-session schedule was insufficient evidence. The missing last row must be represented by a reduced deterministic fixture, not checked in as raw operational data.

## Verified legitimate absences, zeroes and limits

| Case | Evidence-based disposition |
| :--- | :--- |
| Inferred preferred shares = 0 | Existing guarded absence inference, not proof of a directly reported zero. Keep the calculation guard and expose its assumption/lineage. |
| Momentum crossover = 0 | Valid when two eligible SMA pairs have no change in the configured binary relationship; distinguish unavailable transition history. |
| Momentum RSI 0, 50 or 100 | Existing simple-average formula: falling-only, flat-only, rising-only histories respectively. Do not silently replace with Wilder RSI. |
| FCF or growth = 0 | Possible mathematical value; zero/nonpositive CAGR base has explicit nonmeaningful/availability rules. Missing input is never zero. |
| FCF consensus EPS / market capitalization | No evidence-approved mapping exists in the current analyzer; typed unavailable and reason are legitimate. No new provider is authorized. Required forward evidence can make classification indeterminate under existing policy. |
| No direct provider field on an arithmetic mean | Inapplicable parent field; component concepts must remain visible. |
| No fiscal period on a quote/user assumption | Inapplicable, not a failed annual-data lookup. |
| Latest KO OHLC missing | External data-quality condition observed on this date. Rejection is correct; failure presentation is defective. No successful live Momentum run claimed. Valid live history remains required for ESC-C. |
| Historical adjusted prices | Observation-date filtering exists, but provider vintage/adjustment history and an exchange session schedule are not retained. Do not call this proof of full historical knowability. Boundary cases remain required in the matrix. |

The ledger is not frozen against discovery. New discrepancies during implementation must be added with evidence; materially different financial policy must be reviewed before dependent changes. No entry may be closed merely by linking an issue or changing wording while its underlying calculation/data defect persists.
